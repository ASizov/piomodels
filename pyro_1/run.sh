# Esto se supone que solo se corre una vez. Si se re-corre manda error de que ya existe pero no pasa nada
pyro4-ns&

# Esto se supone que solo se corre una vez. Si se re-corre manda error de que ya existe pero no pasa nada
python3 service_creator.py&

python3 client_example.py
